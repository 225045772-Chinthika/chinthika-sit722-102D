name: CI (testing) - Test, Build & Push
on:
  push:
    branches: [ testing ]
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/ci-testing.yml"

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    container: python:3.10

    services:
      product_db:
        image: postgres:14
        env:
          POSTGRES_DB: productdb
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

      order_db:
        image: postgres:14
        env:
          POSTGRES_DB: orderdb
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

      customer_db:
        image: postgres:14
        env:
          POSTGRES_DB: customerdb
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - uses: actions/checkout@v4

      - name: Install Python deps (all services)
        shell: bash
        run: |
          python -m pip install --upgrade pip
          for svc in product_service order_service customer_service; do
            if [ -f backend/$svc/requirements.txt ]; then pip install -r backend/$svc/requirements.txt; fi
            if [ -f backend/$svc/requirements-dev.txt ]; then pip install -r backend/$svc/requirements-dev.txt; fi
          done

      - name: Wait for Postgres services to be ready
        shell: bash
        run: |
          for i in {1..30}; do
            pg_isready -h product_db -U postgres && \
            pg_isready -h order_db -U postgres && \
            pg_isready -h customer_db -U postgres && exit 0
            sleep 2
          done
          echo "Postgres services not ready in time" && exit 1

      - name: Install deps & run backend tests (skip if none)
        shell: bash
        run: |
          declare -A URLS
          URLS[product_service]="postgresql://postgres:postgres@product_db:5432/productdb"
          URLS[order_service]="postgresql://postgres:postgres@order_db:5432/orderdb"
          URLS[customer_service]="postgresql://postgres:postgres@customer_db:5432/customerdb"

          for svc in product_service order_service customer_service; do
            if [ -d "backend/$svc/tests" ]; then
              echo "Running tests in $svc"
              (cd backend/$svc && DATABASE_URL="${URLS[$svc]}" pytest -q) || (echo "Tests failed in $svc" && exit 1)
            else
              echo "No tests for $svc — skipping"
            fi
          done

      - name: Azure login (SP)
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: ACR login
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}
          echo "ACR_LOGIN_SERVER=$(az acr show -n ${{ secrets.ACR_NAME }} --query loginServer -o tsv)" >> $GITHUB_ENV
          echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build & push images (backends + frontend)
        run: |
          for image in product_service order_service customer_service frontend; do
            docker build -t $ACR_LOGIN_SERVER/$image:$IMAGE_TAG $( [[ "$image" == "frontend" ]] && echo frontend || echo backend/$image )
            docker push $ACR_LOGIN_SERVER/$image:$IMAGE_TAG
            # also tag latest for staging
            docker tag $ACR_LOGIN_SERVER/$image:$IMAGE_TAG $ACR_LOGIN_SERVER/$image:latest
            docker push $ACR_LOGIN_SERVER/$image:latest
          done